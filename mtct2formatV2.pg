let
	Source = #"Data Pull Query (External)",
	changingSpecificTypes = Table.TransformColumnTypes(Source,{{"MTCT Buy In", type number}, {"# of seats for Qualifier(GPP)", type text}}),
	addingUkStartTime = Table.AddColumn(changingSpecificTypes, "UK Start Time", each [MTCT Start Time]+#duration(0,5,0,0), type datetime),
    #"Removed Other Columns" = Table.SelectColumns(addingUkStartTime,{"UK Start Time", "MTCT Id", "MTCT Name"})
	
in
	#"Removed Other Columns"